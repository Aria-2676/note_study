这是项目的整体规则，当前版本为V5.0.4
新建一个版本规范，每新增一次验收通过的修改，就为版本号加1或10
每次修改结束后都要输出对应的测试要求，包括功能测试、性能测试、安全测试等确保项目的质量和稳定性
项目应结构化，防止出现一个文件中包含所有代码的情况，每个文件应包含一个功能模块的代码
项目应使用版本控制，如Git，确保代码的版本管理和协作开发。具体要求：
- 使用 .gitignore 忽略构建产物（build/、.gradle/、.idea/等），只追踪源代码
- 每次验收通过后创建一个 commit，commit 信息格式为"V5.0.x: 功能描述"
项目应使用代码规范，Dart项目遵循 Effective Dart 规范，确保代码的质量和可读性。具体要求：
- 遵循 Effective Dart 规范，如命名规范、代码格式化、注释等
- 避免滥用魔法值、硬编码等不好的实践
- 避免滥用全局变量、静态方法等不好的实践
- 使用 Provider 进行状态管理，保持数据流清晰可追踪
项目应有基本的文档说明，如 README.md 包含项目简介、运行方式、依赖说明等
每次版本更新后，应在 README.md 中添加详细的版本历史记录，包括：
- 版本号和更新日期
- 具体修改内容
- 使用的方法和技术
应严格执行要求，不能出现任何违规行为，不为项目引入用户没有说明要加入的UI和功能模块，如果想要添加也应先与用户沟通确认。
用户告知验收通过的时候，帮助其完成版本备份的流程。

## 文档规范

### 1. 功能模块文档（kf.md）
- **文件位置**：项目根目录
- **内容要求**：
  - 按模块划分（任务管理、积分系统、商城系统等）
  - 每个功能包含：
    - 功能描述
    - 核心文件位置
    - 关键函数列表及作用
    - 实现逻辑简述
    - 调用关系（可选）

### 2. API/函数文档
- **使用Dart doc comments**：为关键函数添加文档注释
- **内容包括**：
  - 函数作用
  - 参数说明
  - 返回值说明
  - 调用示例
  - 注意事项

### 3. 架构文档（可选）
- **文件位置**：`docs/architecture.md`
- **内容要求**：
  - 整体架构描述
  - 模块间依赖关系
  - 数据流说明
  - 状态管理方案

### 4. 文档维护
- **与代码同步**：每次修改功能时同步更新文档
- **版本控制**：文档纳入Git管理
- **定期审查**：确保文档准确性和完整性
- **简洁实用**：只记录关键信息，避免文档膨胀

## 功能实现记录（kf.md）
如：
- 任务管理：`lib/pages/task_page.dart`
- 积分系统：`lib/pages/mine_page.dart`（积分显示和管理）
- 商城系统：`lib/pages/shop_page.dart`
- 桌面小组件：`lib/pages/widget_guide_page.dart` 和 `lib/services/widget_service.dart`
- 主题切换：`lib/pages/settings_page.dart`
- 数据存储：`lib/services/database_service.dart`
- 状态管理：`lib/providers/app_provider.dart`